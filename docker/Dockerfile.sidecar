FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml README.md ./
COPY sidecar ./sidecar
COPY migrations ./migrations

RUN pip install --no-cache-dir .

EXPOSE 8000

CMD ["python", "-m", "sidecar.cli", "serve"]
